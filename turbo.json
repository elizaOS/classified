{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["LOG_LEVEL"],
      "outputs": ["dist/**", "dist-backend/**", "dist-binaries/**"]
    },
    "build:binary": {
      "dependsOn": ["build"],
      "env": ["LOG_LEVEL"],
      "outputs": ["dist-binaries/**"]
    },
    "lint": {
      "outputs": [".eslintcache"]
    },
    "format": {
      "outputs": []
    },
    "format:check": {
      "outputs": []
    },
    "start": {
      "env": ["LOG_LEVEL"],
      "persistent": true
    },
    "dev": {
      "persistent": true
    },
    "test": {
      "outputs": ["coverage/**"],
      "inputs": ["src/**/*.ts", "src/**/*.js", "__tests__/**/*", "*.test.*"]
    },
    "migrate": {
      "cache": false
    },
    "migrate:generate": {
      "cache": false
    },
    "clean": {
      "cache": false
    }
  }
}
